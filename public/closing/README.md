# Closing screen

`discord-qr.png` encodes the Poimandres Discord invite, https://discord.gg/poimandres, as black modules on a transparent ground so the screen's cyan backdrop supplies the contrast and quiet zone. Rebuild it with `pnpm bake:discord-qr` (`scripts/bake-discord-qr.mjs`) if the invite changes.
