# Charter announcement

`charter-post.svg` is a vector reconstruction of the visible text and layout in the screenshot supplied in chat on 2026-09-10. It is a preview, not the original screenshot.

Replace the texture URL in `src/view/renderers/announcement-renderer.tsx` with the original screenshot when its file is available. The frame and collapse capture follow the image aspect ratio automatically.
